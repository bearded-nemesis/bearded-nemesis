%h1 Listing users

%table
  %tr
    %th
    %th Email Address
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %th= image_tag avatar_url(user, 24)
      %td= link_to user.email, user
      %td= link_to "Add Friend", friendships_path(:friend_id => user), :method => :post
      %td= link_to 'Edit', edit_user_path(user) if is_user_admin?
      %td= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }if is_user_admin?

%br
