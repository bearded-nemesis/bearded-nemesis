%h1 Listing users

%table
  %tr
    %th
    %th Email Address
    %th

  - @users.each do |user|
    %tr
      %th= image_tag avatar_url(user, 24)
      %td= link_to user.email, user
      %td= add_friend_link user

%br
