%h1 Listing users

%table
  %tr
    %th
    %th Name
    %th Email Address
    %th

  - @users.each do |user|
    %tr
      %td= image_tag avatar_url(user, 24)
      %td= link_to user.display_name, user
      %td= add_friend_link user

%br
