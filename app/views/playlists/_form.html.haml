= form_for @playlist do |f|
  - if @playlist.errors.any?
    #error_explanation
      %h2= "#{pluralize(@playlist.errors.count, "error")} prohibited this playlist from being saved:"
      %ul
        - @playlist.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :user
    = f.text_field :user
  .field
    = f.label :amountOfSongs
    = f.text_field :amountOfSongs
  .field
    = f.label :includeUnratedSongs
    = f.text_field :includeUnratedSongs
  .field
    = f.label :unratedSongsRating
    = f.text_field :unratedSongsRating
  .actions
    = f.submit 'Save'
